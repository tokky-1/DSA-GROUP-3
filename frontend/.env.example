# Leave blank to use the Vite dev proxy (/api/* → http://localhost:8000).
# Set to a full URL (e.g. https://api.example.com) only when pointing at a remote server.
VITE_API_BASE_URL=

# Set to 'false' to hit the real backend. Any other value (or absent) uses mock mode.
VITE_MOCK_API=false
